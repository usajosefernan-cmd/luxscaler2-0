name: Deploy to Replicate

on:
  push:
    branches: [main]
    paths:
      - 'predict.py'
      - 'cog.yaml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Cog
        uses: replicate/setup-cog@v2
        with:
          token: ${{ secrets.REPLICATE_API_TOKEN }}
      
      - name: Push to Replicate
        run: |
          cog push r8.im/usajosefernan-cmd/luxscaler2-0 --env GEMINI_API_KEY=AIzaSyCE3dBuBcDAyTtj8AaoLQVSQw4ryn4N2jc
        env:
          REPLICATE_API_TOKEN: ${{ secrets.REPLICATE_API_TOKEN }}
          GEMINI_API_KEY: AIzaSyCE3dBuBcDAyTtj8AaoLQVSQw4ryn4N2jc
